## 2. 영업 포털 (EXEC)

> **대상 독자**: 알티바이오 영업팀 담당자 (UAT 수행자)
> **접속 URL**: `http://localhost:3000/exec`
> **로그인 계정**: `sales1@rtbio.com` / 비밀번호는 **관리자가 안내한 비밀번호**를 입력하세요.
> **권한(Role)**: EXEC (영업팀)

### 공통 안내
- 로그인 후 좌측 **사이드바**에 다음 메뉴가 보이면 정상입니다.
  - **영업** 그룹: `영업 대시보드`, `영업 현황`, `거래처 관리`, `담당자 관리`, `학회 관리`, `영업사원 계정`
  - **기타** 그룹: `📢 공지사항`, `사용량 입력`, `보고서 작성`, `데이터 탐색기`
  - (`직원 관리` 메뉴는 **팀 관리자/메타 관리자 권한이 있는 계정에서만** 보입니다. 일반 영업 계정은 보이지 않는 것이 정상입니다.)
- 사이드바 상단에 로그인 사용자명(`김영업` 등)과 역할(`영업팀`)이 표시됩니다.
- 사이드바 하단에 **모드 전환 드롭다운**(관리자/박진우/신현호/이경동)이 있습니다. 본 가이드의 일부 테스트는 이 모드 값에 따라 화면이 달라집니다.
- 화면 우하단(또는 상단)에 나타나는 작은 알림 메시지를 **토스트(toast)** 라고 부릅니다. 저장·삭제 등 동작 직후 토스트가 떠야 정상입니다.
- 본 포털은 프로토타입 단계로, 일부 데이터는 **새로고침 시 초기화**되는 Mock 데이터입니다(각 테스트에 별도 표시).

---

### 2.1 로그인 및 진입

- **목적**: 영업 포털에 정상 로그인하고 첫 화면(영업 대시보드)이 뜨는지 확인한다.
- **사전조건**: 관리자에게서 `sales1@rtbio.com` 계정의 비밀번호를 안내받았다.
- **테스트 단계**:
  1. 브라우저 주소창에 `http://localhost:3000/exec` 를 입력하고 이동한다.
  2. 로그인 화면이 나오면 아이디 `sales1@rtbio.com`, 비밀번호(관리자 안내값)를 입력하고 로그인한다.
  3. 좌측 사이드바와 우측 본문이 표시되는지 확인한다.
- **기대 결과**: 좌측 사이드바에 "영업/기타" 메뉴가 표시되고, 우측에 **영업 대시보드** 화면(상단 KPI 카드 4장 + 위젯 영역)이 나타난다.
- **Pass 기준**: 영업 대시보드가 로딩되고, 사이드바 사용자명/역할이 표시되면 합격. 로그인 실패하거나 빈 화면이면 불합격.

---

### 2.2 영업 대시보드 — 상단 KPI 카드

- **목적**: 월 매출/수금/미수금/담당 거래처 KPI가 실제 데이터로 표시되는지 확인한다.
- **사전조건**: 영업 포털 로그인 완료.
- **테스트 단계**:
  1. 좌측 메뉴 **`영업 대시보드`** 를 클릭한다.
  2. 상단 KPI 카드 4장을 확인한다: **월 매출 현황 / 월 수금액 현황 / 월 미수금 현황 / 담당 거래처**.
- **기대 결과**:
  - `월 매출 현황`: 최신 정산월의 누적 매출 금액(원)과 "YYYY-MM 누적" 부제가 표시된다.
  - `월 수금액 현황`: 해당 월 수금액과 부제가 표시된다.
  - `월 미수금 현황`: 미수금 합계와 "거래처 N개" 부제가 표시된다.
  - `담당 거래처`: 활성 거래처 수와 "대리점 N / 병원 N" 부제가 표시된다.
- **Pass 기준**: 4장 카드 모두 숫자(₩ 금액 또는 개수)가 채워지고 "—"나 깨진 값이 없으면 합격.

---

### 2.3 영업 대시보드 — 위젯 추가

- **목적**: 대시보드에 위젯을 새로 추가할 수 있는지 확인한다.
- **사전조건**: 영업 대시보드 화면.
- **테스트 단계**:
  1. 우측 상단 **`➕ 위젯 추가`** 버튼을 클릭한다.
  2. "위젯 추가" 모달이 열리면 **`갤러리에서 선택`** 영역에서 위젯 카드 하나를 클릭한다.
  3. 모달이 닫히면 대시보드 그리드에 새 위젯이 추가됐는지 확인한다.
- **기대 결과**: 선택한 위젯이 대시보드 그리드 영역에 카드 형태로 표시된다.
- **Pass 기준**: 위젯이 그리드에 나타나면 합격. 모달이 안 열리거나 추가가 반영되지 않으면 불합격.

---

### 2.4 영업 대시보드 — 위젯 직접 만들기 (커스텀 위젯)

- **목적**: 데이터 소스/집계/필터를 직접 지정해 커스텀 위젯을 만들 수 있는지 확인한다.
- **사전조건**: "위젯 추가" 모달이 열린 상태.
- **테스트 단계**:
  1. `➕ 위젯 추가` → "위젯 추가" 모달에서 **`+ 직접 만들기`** 버튼을 클릭한다.
  2. "위젯 직접 만들기" 모달에서 **위젯 제목**(예: 이번 달 매출)을 입력한다.
  3. **데이터 소스**와 **차트 종류**를 선택한다.
  4. **측정값(집계)**, 필요 시 **분류(그룹)**, **필터 조건**(`+ 조건` 버튼), **정렬/최대 행**을 설정한다.
  5. 우측 **미리보기** 영역에 실데이터 미리보기가 뜨는지 확인한다.
  6. 하단 **`저장`** 버튼을 클릭한다.
- **기대 결과**: 소스·측정값을 선택하면 우측 미리보기가 채워지고, 저장 시 대시보드에 새 위젯이 추가된다.
- **Pass 기준**: 미리보기가 표시되고 저장 후 위젯이 대시보드에 나타나면 합격.

---

### 2.5 영업 대시보드 — 위젯 수정/복제/새로고침/삭제 (위젯 메뉴)

- **목적**: 위젯별 컨텍스트 메뉴 동작을 확인한다.
- **사전조건**: 대시보드에 위젯이 1개 이상 있다.
- **테스트 단계**:
  1. 대시보드의 위젯에서 메뉴 버튼(⋮ / 우클릭 등)으로 **위젯 컨텍스트 메뉴**를 연다.
  2. 메뉴 항목을 차례로 확인: **`✏ 위젯 수정`**, **`복제`**, **`새로고침`**, **`삭제`**.
  3. **`✏ 위젯 수정`** 클릭 → "위젯 수정" 모달에서 제목을 바꾸고 **`저장`** 한다.
  4. **`복제`** 클릭 → 같은 위젯이 하나 더 생기는지 확인한다.
  5. **`새로고침`** 클릭 → 위젯 데이터가 다시 그려지는지 확인한다.
  6. **`삭제`** 클릭 → 위젯이 제거되는지 확인한다.
- **기대 결과**: 각 메뉴 동작(제목 변경/복제/새로고침/삭제)이 즉시 화면에 반영된다.
- **Pass 기준**: 4개 동작이 모두 의도대로 반영되면 합격. (복제/삭제는 위젯 수 변화로 판정)

---

### 2.6 영업 대시보드 — 레이아웃 내보내기/가져오기/초기화 (⋮ 더보기 메뉴)

- **목적**: 대시보드 레이아웃 백업·복원·초기화 기능을 확인한다.
- **사전조건**: 영업 대시보드 화면.
- **테스트 단계**:
  1. 제목 "영업 대시보드" 옆 **`⋮`(더보기)** 버튼을 클릭한다.
  2. 메뉴에서 **`대시보드 내보내기`** 클릭 → JSON 파일이 내려받아지는지 확인한다.
  3. 다시 `⋮` → **`대시보드 가져오기`** 클릭 → 방금 받은 JSON 파일을 선택해 불러온다.
  4. 다시 `⋮` → **`기본 레이아웃으로 초기화`** 클릭 → 레이아웃이 기본값으로 돌아가는지 확인한다.
- **기대 결과**: 내보내기 시 `.json` 파일 다운로드, 가져오기 시 레이아웃 복원, 초기화 시 기본 레이아웃 복귀.
- **Pass 기준**: 세 동작이 각각 정상 수행되면 합격.

---

### 2.7 영업 현황 — 기간/유형 필터 조회

- **목적**: 기간과 거래처 유형 기준으로 매출 현황을 조회할 수 있는지 확인한다.
- **사전조건**: 영업 포털 로그인 완료.
- **테스트 단계**:
  1. 좌측 메뉴 **`영업 현황`** 을 클릭한다.
  2. 상단 필터에서 **시작일**, **종료일**을 지정한다.
  3. **유형** 드롭다운에서 `전체 / 병원만 / 대리점만 / 기타만` 중 하나를 선택한다.
  4. **`조회`** 버튼을 클릭한다.
- **기대 결과**: 부제(소제목)에 선택한 기간과 유형이 반영되고(`YYYY-MM-DD ~ YYYY-MM-DD · 유형`), 아래 KPI 카드/차트/표가 갱신된다.
- **Pass 기준**: 조회 후 소제목이 선택값으로 바뀌고 화면이 갱신되면 합격.

---

### 2.8 영업 현황 — 빠른 기간 버튼 (오늘/이번 주/이번 달)

- **목적**: 빠른 기간 버튼으로 날짜가 자동 설정되는지 확인한다.
- **사전조건**: `영업 현황` 화면.
- **테스트 단계**:
  1. 필터 우측의 **`오늘`** 버튼을 클릭한다 → 시작일·종료일이 오늘 날짜로 설정된다.
  2. **`이번 주`** 버튼을 클릭한다 → 시작일이 7일 전, 종료일이 오늘로 설정된다.
  3. **`이번 달`** 버튼을 클릭한다 → 시작일이 해당 월 1일, 종료일이 오늘로 설정된다.
- **기대 결과**: 각 버튼 클릭 시 시작/종료일이 규칙대로 채워지고 즉시 조회가 다시 실행된다.
- **Pass 기준**: 3개 버튼이 모두 날짜를 올바르게 채우고 화면을 갱신하면 합격.

---

### 2.9 영업 현황 — KPI 및 병원/대리점 매출 차트

- **목적**: 매출 KPI 4장과 병원/대리점 매출 막대그래프가 표시되는지 확인한다.
- **사전조건**: `영업 현황` 화면, 조회 완료.
- **테스트 단계**:
  1. `영업 현황` 화면에서 KPI 카드 4장(**총 매출 / 병원 매출 / 대리점 매출 / 신규 거래처**)을 확인한다.
  2. 그 아래 **병원 매출** 차트와 **대리점 매출** 차트(막대그래프)가 그려지는지 확인한다.
- **기대 결과**: KPI 4장 모두 금액/개수가 표시되고, 두 막대그래프가 정상 렌더링된다.
- **Pass 기준**: KPI와 차트가 모두 표시되면 합격. 차트가 빈 캔버스이거나 KPI가 비면 불합격.

---

### 2.10 영업 현황 — 담당자별 매출 표 + 드릴다운 필터

- **목적**: 담당자별 매출 표에서 담당자를 클릭해 품목 드릴다운을 필터링하는 동작을 확인한다.
- **사전조건**: `영업 현황` 화면.
- **테스트 단계**:
  1. **담당자별 매출** 표에서 컬럼(담당자/역할/담당 거래처/발주 건수/매출액/**반품건수**/신규)을 확인한다.
  2. 담당자 행(예: `박진우`)을 클릭한다.
  3. 화면 아래 **품목/부위/사이즈 드릴다운** 영역에 해당 담당자 기준 안내 배너(담당 비중 %)가 뜨는지 확인한다.
  4. 배너의 **`✕ 필터 해제`** 또는 같은 담당자 행을 다시 클릭해 필터를 해제한다.
- **기대 결과**: 담당자 클릭 시 해당 행이 선택 표시(테두리/배경)되고 드릴다운 수치가 비중만큼 축소 반영, 토스트로 "OOO 담당자 기준 드릴다운" 안내가 뜬다. 해제 시 전체 기준으로 복귀.
- **Pass 기준**: 선택/해제 토글이 동작하고 드릴다운 수치가 바뀌면 합격.

---

### 2.11 영업 현황 — 품목/부위/사이즈 드릴다운 펼치기

- **목적**: 브랜드별 매출을 부위/사이즈 단위로 펼쳐볼 수 있는지 확인한다.
- **사전조건**: `영업 현황` 화면 하단 드릴다운 영역.
- **테스트 단계**:
  1. 드릴다운에서 브랜드 행(예: `리코트 플러스`)의 `▶` 토글을 클릭한다.
  2. 부위(상지/하지 등) 및 사이즈별 수량/금액 표가 펼쳐지는지 확인한다.
  3. 다시 클릭해 접는다.
- **기대 결과**: 클릭 시 `▶`→`▼` 로 바뀌며 하위 표가 펼쳐지고, 재클릭 시 접힌다.
- **Pass 기준**: 펼침/접힘이 정상 토글되면 합격.

---

### 2.12 영업 현황 — 모드 전환에 따른 표시 변화

- **목적**: 사이드바 모드(관리자/개별 사원)에 따라 담당자별 표시가 달라지는지 확인한다.
- **사전조건**: `영업 현황` 화면.
- **테스트 단계**:
  1. 사이드바 하단 **모드 드롭다운**에서 `이경동 (사원)` 등 개별 사원을 선택한다.
  2. "모드 전환: OOO" 토스트가 뜨는지 확인한다.
  3. 담당자별 매출 표에서 본인 외 담당자 행이 흐리게(opacity 낮게) 표시되는지 확인한다.
  4. 다시 `관리자 (팀장)` 로 되돌린다.
- **기대 결과**: 개별 사원 모드에서는 해당 사원 행만 진하게, 나머지는 흐리게 표시된다. 관리자 모드에서는 전부 진하게 표시된다.
- **Pass 기준**: 모드 변경 시 토스트 + 표시 변화가 반영되면 합격.

---

### 2.13 거래처 관리 — 목록/검색

- **목적**: 거래처 목록 조회와 검색을 확인한다. (경영지원팀과 동일한 통합 폼 사용)
- **사전조건**: 영업 포털 로그인 완료, 거래처 데이터가 시드되어 있다.
- **테스트 단계**:
  1. 좌측 메뉴 **`거래처 관리`** 를 클릭한다.
  2. 거래처 목록(표 또는 카드)이 표시되는지 확인한다.
  3. 상단 검색창에 거래처명 일부(예: `한빛`)를 입력한다.
- **기대 결과**: 거래처 목록이 로드되고, 검색어에 맞는 거래처만 필터링되어 표시된다.
- **Pass 기준**: 목록이 표시되고 검색이 동작하면 합격.

---

### 2.14 거래처 관리 — 거래처 상세 보기

- **목적**: 거래처 상세 정보(담당 영업사원/미수금/누적 매출/취급 품목/품목별 판매수량 등)를 확인한다.
- **사전조건**: 거래처 목록이 표시된 상태.
- **테스트 단계**:
  1. `거래처 관리` 목록에서 거래처 한 곳을 선택해 상세를 연다.
  2. 상세 모달/화면에서 거래처명·유형·담당 영업사원·결제 조건·미수금·누적 매출·의사 수·지역/주소·담당자 정보를 확인한다.
  3. **취급 품목** 배지, **품목별 판매수량(이번 달)** 표를 확인한다.
- **기대 결과**: 거래처 기본 정보와 함께 이번 달 품목별 판매수량/판매액 표(합계 행 포함)가 표시된다. 발주 내역이 없으면 "이번 달 발주 내역이 없습니다" 안내가 뜬다.
- **Pass 기준**: 상세 정보와 품목별 표가 정상 표시되면 합격.

---

### 2.15 거래처 관리 — 경쟁업체/특이사항 비고 저장

- **목적**: 거래처 상세의 경쟁업체 메모(비고)를 저장하는 동작을 확인한다.
- **사전조건**: 거래처 상세 모달이 열린 상태.
- **테스트 단계**:
  1. 거래처 상세 하단 **경쟁업체 품목(비고)** 텍스트 영역에 메모를 입력한다(예: "경쟁사 X 실리콘 밴드 20%").
  2. 모달 하단의 **`비고 저장`** 버튼을 클릭하거나, 텍스트 영역 밖을 클릭(blur)한다.
- **기대 결과**: "비고 저장 완료" 토스트가 뜨고, 다시 상세를 열어도 입력한 메모가 유지된다. (서버 저장 — Client.note)
- **Pass 기준**: 저장 토스트가 뜨고 재진입 시 메모가 보존되면 합격. 저장 실패 토스트가 뜨면 불합격.

---

### 2.16 거래처 관리 — 발주서 출력

- **목적**: 거래처 발주서를 인쇄용 창으로 출력하는 동작을 확인한다.
- **사전조건**: 거래처 상세 모달이 열린 상태. **브라우저 팝업 차단 해제 권장.**
- **테스트 단계**:
  1. 거래처 상세 하단 **`발주서 출력`** 버튼을 클릭한다.
  2. 새 창(인쇄용 발주서)이 열리는지 확인한다.
- **기대 결과**: "발 주 서" 제목, 공급자(알티바이오)/공급받는자(거래처) 정보, 품목·수량·단가·금액·발주일 표가 포함된 새 창이 뜬다. 하단에 `인쇄`/`닫기` 버튼이 있다.
- **Pass 기준**: 발주서 창이 열리고 거래처 정보·품목 표가 채워지면 합격. 팝업이 막히면 "팝업을 허용해주세요" 토스트가 뜨므로 차단을 해제하고 재시도.

---

### 2.17 거래처 관리 — 거래명세표 출력

- **목적**: 거래처 거래명세표(공급가액/부가세/총합계 포함)를 출력하는 동작을 확인한다.
- **사전조건**: 거래처 상세 모달이 열린 상태. 팝업 차단 해제 권장.
- **테스트 단계**:
  1. 거래처 상세 하단 **`거래명세표 출력`** 버튼을 클릭한다.
  2. 새 창(거래명세표)이 열리는지 확인한다.
- **기대 결과**: "거래명세표" 제목, 공급자/공급받는자 정보, 거래일·품목·수량·단가·공급가액 표, **공급가액 합계 / 부가세(10%) / 총 합계** 행이 표시된다.
- **Pass 기준**: 거래명세표 창이 열리고 합계 3행이 계산되어 표시되면 합격.

---

### 2.18 담당자 관리 — 담당자 카드 조회

- **목적**: 영업사원별 담당 거래처 카드를 조회한다.
- **사전조건**: 영업 포털 로그인 완료, 모드는 `관리자 (팀장)` 권장.
- **테스트 단계**:
  1. 좌측 메뉴 **`담당자 관리`** 를 클릭한다.
  2. 영업사원별 카드(이름/역할/담당 거래처 수 + 담당 거래처 목록)가 표시되는지 확인한다.
- **기대 결과**: 박진우/신현호/이경동 등 담당자 카드가 그리드로 표시되고, 각 카드에 담당 거래처 목록이 보인다.
- **Pass 기준**: 담당자 카드가 표시되면 합격. (개별 사원 모드에서는 본인 카드만 표시되는 것이 정상)

---

### 2.19 담당자 관리 — 거래처 배정

- **목적**: 특정 담당자에게 거래처를 배정하는 동작을 확인한다.
- **사전조건**: `담당자 관리` 화면, 모드 `관리자 (팀장)`.
- **테스트 단계**:
  1. 우측 상단 **`+ 거래처 배정`** 버튼(또는 카드 내 `+ 거래처 배정`)을 클릭한다.
  2. "거래처 배정" 모달에서 **담당자**와 **거래처**를 선택한다.
  3. 모달 확인 버튼을 클릭한다.
- **기대 결과**: "OOO 거래처 → OOO에게 배정" 토스트가 뜨고, 해당 담당자 카드의 담당 거래처 목록에 추가된다.
- **Pass 기준**: 배정 후 토스트 + 카드 목록 반영되면 합격. (Mock 데이터 — 새로고침 시 초기화)

---

### 2.20 담당자 관리 — 거래처 배정 해제

- **목적**: 담당자에게서 거래처 배정을 제거하는 동작을 확인한다.
- **사전조건**: `담당자 관리` 화면, 담당 거래처가 1개 이상 있는 담당자 카드, 모드 `관리자 (팀장)`.
- **테스트 단계**:
  1. 담당자 카드의 담당 거래처 행 오른쪽 **`✕`** 버튼을 클릭한다.
  2. "OOO를 담당에서 제외할까요?" 확인창에서 **확인**을 누른다.
- **기대 결과**: "배정 해제됨" 토스트가 뜨고 해당 거래처가 목록에서 제거된다.
- **Pass 기준**: 확인 후 거래처가 제거되고 토스트가 뜨면 합격.

---

### 2.21 학회 관리 — 학회 목록/KPI 조회

- **목적**: 학회 목록과 방명록 KPI를 조회한다.
- **사전조건**: 영업 포털 로그인 완료.
- **테스트 단계**:
  1. 좌측 메뉴 **`학회 관리`** 를 클릭한다.
  2. 상단 KPI 카드(**총 방명록 / 접촉 완료 / 신규 거래처 / 미접촉**)를 확인한다.
  3. "최근 학회" 표에서 학회명·종류·날짜·부스비·설치비·방명록·접촉·신규거래·성공률·담당자 배분 컬럼을 확인한다.
- **기대 결과**: KPI 4장과 학회 표가 표시된다. 성공률은 `신규거래 / 접촉`으로 계산된다.
- **Pass 기준**: KPI와 학회 표가 모두 표시되면 합격.

---

### 2.22 학회 관리 — 학회 등록

- **목적**: 새 학회를 등록하는 동작을 확인한다.
- **사전조건**: `학회 관리` 화면.
- **테스트 단계**:
  1. 우측 상단 **`+ 학회 등록`** 버튼을 클릭한다.
  2. "학회 등록" 모달에서 **학회명**, **종류**(정형외과/신경외과/재활의학/기타), **날짜**, **방명록 건수**를 입력한다.
  3. 모달 확인 버튼을 클릭한다.
- **기대 결과**: "학회가 등록되었습니다" 토스트가 뜨고, 학회 표 맨 위에 새 학회가 추가된다(담당자 3명에게 자동 배분).
- **Pass 기준**: 학회가 목록 상단에 추가되면 합격. (Mock — 새로고침 시 초기화)

---

### 2.23 학회 관리 — 학회 방명록(방문자) 추가

- **목적**: 학회 방명록에 방문자를 추가하는 동작을 확인한다.
- **사전조건**: `학회 관리` 화면, 학회 목록이 있다.
- **테스트 단계**:
  1. "최근 학회" 표에서 학회명(밑줄 링크)을 클릭해 **방명록 모달**을 연다.
  2. "➕ 방문자 추가" 영역에서 **이름**, **연락처**, **소속(병원/기관)**, **담당자**, **상태**(미접촉/접촉완료/팔로우업/신규계약/거절), **비고**를 입력한다.
  3. **`추가`** 버튼을 클릭한다.
- **기대 결과**: "방문자가 추가되었습니다" 토스트가 뜨고, 아래 방명록 표에 행이 추가된다. 학회 통계(방문자/접촉/신규)가 갱신된다. (서버 연동 시도, 실패하면 Mock 모드로 동작)
- **Pass 기준**: 방문자 행이 표에 추가되면 합격. 이름/소속 미입력 시 "이름/소속을 입력해주세요" 토스트가 뜨면 검증도 정상.

---

### 2.24 학회 관리 — 학회 방명록(방문자) 삭제

- **목적**: 방명록에서 방문자 행을 삭제하는 동작을 확인한다.
- **사전조건**: 방명록 모달에 방문자가 1명 이상 있다.
- **테스트 단계**:
  1. 방명록 모달의 방문자 행 오른쪽 **`삭제`** 버튼을 클릭한다.
- **기대 결과**: "방문자가 삭제되었습니다" 토스트가 뜨고 해당 행이 제거되며, 학회 통계가 갱신된다.
- **Pass 기준**: 행이 제거되고 토스트가 뜨면 합격.

---

### 2.25 영업사원 계정 — 마스터 카드/실적 조회

- **목적**: 영업사원별 담당 거래처/월 매출/목표 달성률/인센티브를 조회한다.
- **사전조건**: 영업 포털 로그인 완료.
- **테스트 단계**:
  1. 좌측 메뉴 **`영업사원 계정`** 을 클릭한다.
  2. 상단 사원 카드(이름/이메일/담당 거래처 수/4월 매출/목표 달성률 막대/인센티브 배지)를 확인한다.
- **기대 결과**: 사원 카드가 그리드로 표시되고, 목표 초과 사원에는 "🎁 인센티브: N원 (초과분 10%)" 배지가 표시된다.
- **Pass 기준**: 사원 카드와 달성률/인센티브가 표시되면 합격.

---

### 2.26 영업사원 계정 — 사원 상세 + 거래처별 발주 진입

- **목적**: 사원 상세(월별 매출 추이/목표/거래처별 매출)와 거래처 발주 진입을 확인한다.
- **사전조건**: `영업사원 계정` 화면.
- **테스트 단계**:
  1. 사원 카드를 클릭한다.
  2. 하단 "선택한 사원 상세" 영역에 월별 매출 추이/월 목표/연락처/거래처별 표가 표시되는지 확인한다.
  3. 거래처별 표의 **`발주`** 버튼을 클릭한다.
- **기대 결과**: 사원 상세 표가 채워지고, `발주` 클릭 시 "OOO 계정으로 발주 화면 이동" 토스트와 함께 새 창(거래처 포털)이 열린다.
- **Pass 기준**: 사원 상세가 표시되고, 발주 버튼이 거래처 포털 새 창을 여는 것까지 확인되면 합격.

---

### 2.27 공지사항 — 목록 조회

- **목적**: 영업팀 공지사항 발송 이력 화면을 조회한다.
- **사전조건**: 영업 포털 로그인 완료.
- **테스트 단계**:
  1. 좌측 메뉴 **`📢 공지사항`** 을 클릭한다.
  2. 공지 목록(발송 이력)과 작성자 표시(`영업팀`)를 확인한다.
- **기대 결과**: 공지 목록 페이지가 로드되고, 각 공지에 미리보기/편집/삭제 버튼이 보인다.
- **Pass 기준**: 공지 목록이 표시되면 합격.

---

### 2.28 공지사항 — 공지 작성/발송

- **목적**: 거래처/내부 대상으로 공지를 작성·발송하는 동작을 확인한다.
- **사전조건**: `📢 공지사항` 화면.
- **테스트 단계**:
  1. 우측 상단 **`＋ 공지 작성`** 버튼을 클릭한다.
  2. "공지사항 작성 — 영업팀" 모달에서 **제목/내용**과 **발송 대상**을 선택한다.
  3. 발송 대상을 **`특정 업체`** 로 선택하면 검색/체크박스에서 업체를 1곳 이상 고른다. (`검색결과 전체선택`/`전체해제` 보조 버튼 사용 가능)
  4. 작성자(`영업팀`)가 자동 설정된 것을 확인하고 발송 버튼을 클릭한다.
- **기대 결과**: "공지사항이 발송되었습니다" 토스트가 뜨고 목록에 새 공지가 추가된다.
- **Pass 기준**: 발송 후 목록에 공지가 추가되면 합격. (특정 업체에서 0건 선택 시 발송이 막히고 안내가 뜨면 검증도 정상)

---

### 2.29 공지사항 — 미리보기/편집/삭제

- **목적**: 기존 공지의 미리보기/편집/삭제가 각각 분리 동작하는지 확인한다.
- **사전조건**: 공지가 1건 이상 있다.
- **테스트 단계**:
  1. 공지 행의 **`미리보기`** 클릭 → 공지 상세 미리보기가 뜨는지 확인한다.
  2. **`편집`** 클릭 → 편집 모달에서 제목/내용을 수정하고 저장한다.
  3. **`삭제`** 클릭 → 확인 후 공지가 제거되는지 확인한다.
- **기대 결과**: 미리보기는 조회만, 편집은 수정 반영, 삭제는 제거. (편집이 삭제로 동작하지 않아야 함)
- **Pass 기준**: 세 버튼이 각자 의도대로 동작하면 합격.

---

### 2.30 사용량 입력 — 거래처 선택 및 입력표 조회

- **목적**: 거래처별 실사용 수량 입력표가 표시되는지 확인한다.
- **사전조건**: 영업 포털 로그인 완료.
- **테스트 단계**:
  1. 좌측 메뉴 **`사용량 입력`** 을 클릭한다.
  2. **거래처 선택** 드롭다운(병원만 표시)에서 거래처를 선택한다.
  3. 입력표(제품명/사이즈/전월사용/당월사용/단가/소계)와 **마감 기간**이 표시되는지 확인한다.
- **기대 결과**: 선택 거래처의 입력표가 표시되고, 마감 기간 라벨이 갱신된다.
- **Pass 기준**: 거래처 선택 시 입력표가 갱신되면 합격.

---

### 2.31 사용량 입력 — 당월사용 수량 수정 및 소계/합계 자동 계산

- **목적**: 당월사용 수량을 입력하면 소계·합계가 자동 갱신되는지 확인한다.
- **사전조건**: `사용량 입력` 화면, 입력표가 표시된 상태.
- **테스트 단계**:
  1. 임의 행의 **당월사용** 입력칸 숫자를 변경한다(예: 3 → 5).
  2. 해당 행의 **소계**가 즉시 바뀌는지 확인한다.
  3. 표 하단 **합계** 및 "품목별 사용량·판매액(전체 거래처 합계)" 요약 표가 갱신되는지 확인한다.
- **기대 결과**: 당월사용 변경 시 소계 = 당월사용 × 단가, 합계와 전체 요약 표가 자동 갱신된다.
- **Pass 기준**: 소계/합계/요약이 입력값에 맞게 자동 계산되면 합격.

---

### 2.32 사용량 입력 — 임시저장

- **목적**: 입력 중인 사용량을 임시저장하는 동작을 확인한다.
- **사전조건**: `사용량 입력` 화면, 당월사용 수량이 입력된 상태.
- **테스트 단계**:
  1. 화면 우하단 **`임시저장`** 버튼을 클릭한다.
- **기대 결과**: "임시 저장되었습니다" 토스트가 뜬다. (서버 저장 — `/api/data-usage` upsert. 네트워크/인증 문제 시 "오프라인 모드"로 안내)
- **Pass 기준**: 저장 토스트가 뜨면 합격. 저장할 항목이 없으면 "저장할 항목이 없습니다" 안내가 뜨는 것도 정상.

---

### 2.33 사용량 입력 — 제출

- **목적**: 사용량을 최종 제출하는 동작을 확인한다.
- **사전조건**: `사용량 입력` 화면, 당월사용 수량이 입력된 상태.
- **테스트 단계**:
  1. 우하단 **`제출`** 버튼을 클릭한다.
  2. "사용량 제출" 확인 모달("제출 후 수정이 제한됩니다")에서 확인을 클릭한다.
- **기대 결과**: "사용량이 제출되었습니다" 토스트가 뜬다. (서버 저장 시도, 실패 시 "오프라인 모드" 안내)
- **Pass 기준**: 확인 후 제출 토스트가 뜨면 합격.

---

### 2.34 사용량 입력 — 이전 제출 내역 확인

- **목적**: 과거 제출 내역 표가 표시되는지 확인한다.
- **사전조건**: `사용량 입력` 화면.
- **테스트 단계**:
  1. 화면 하단 **`이전 제출 내역`** 표를 확인한다(기간/거래처/항목수/금액/상태).
- **기대 결과**: 제출완료 배지가 달린 과거 내역 행들이 표시된다.
- **Pass 기준**: 이전 제출 내역 표가 표시되면 합격.

---

### 2.35 보고서 작성 — 양식 선택 및 미리보기

- **목적**: ISO 13485 품질양식을 선택해 미리보기가 뜨는지 확인한다.
- **사전조건**: 영업 포털 로그인 완료.
- **테스트 단계**:
  1. 좌측 메뉴 **`보고서 작성`** 을 클릭한다.
  2. 상단 **양식 선택** 드롭다운에서 양식 하나를 선택한다.
  3. 왼쪽 본문에 양식 미리보기가 나타나는지 확인한다.
- **기대 결과**: 양식 선택 시 "양식을 선택하면 미리보기가 나타납니다" 안내가 양식 폼으로 바뀐다.
- **Pass 기준**: 양식이 선택되고 미리보기 폼이 표시되면 합격.

---

### 2.36 보고서 작성 — AI 어시스턴트 자동 채움

- **목적**: 자연어 입력으로 양식을 자동 채우는 AI 어시스턴트를 확인한다.
- **사전조건**: `보고서 작성` 화면, 양식이 선택된 상태.
- **테스트 단계**:
  1. 우측 **AI 어시스턴트** 입력창에 예시 문구(예: "한빛정형외과로 발주서 작성")를 입력한다.
  2. **`↑`(전송)** 버튼을 클릭한다.
  3. (제공되는 경우) 추천 문구(suggestions)를 클릭해도 본다.
- **기대 결과**: AI 메시지 영역에 응답이 표시되고, 관련 양식 항목이 자동으로 채워진다.
- **Pass 기준**: 메시지 전송 후 응답/채움 동작이 일어나면 합격. (백엔드 미연동 시 응답이 제한될 수 있으므로, 입력/전송 UI 동작 여부로 1차 판정)

---

### 2.37 보고서 작성 — 임시저장 / 인쇄 / PDF 출력

- **목적**: 보고서 양식의 저장·인쇄·PDF 출력 버튼을 확인한다.
- **사전조건**: `보고서 작성` 화면, 양식이 채워진 상태.
- **테스트 단계**:
  1. 하단 **`임시저장`** 버튼을 클릭한다.
  2. **`양식대로 인쇄`** 버튼을 클릭한다 → 인쇄 미리보기/대화상자가 뜨는지 확인한다.
  3. **`PDF 출력`** 버튼을 클릭한다 → PDF가 생성/다운로드되는지 확인한다.
- **기대 결과**: 임시저장은 저장 처리, 인쇄는 인쇄 대화상자, PDF는 파일 출력이 각각 동작한다.
- **Pass 기준**: 세 버튼이 각각 의도된 동작(저장/인쇄/PDF)을 수행하면 합격.

---

### 2.38 데이터 탐색기 — 테이블 선택/조회

- **목적**: 원본 DB 테이블을 선택해 데이터를 조회하는 동작을 확인한다.
- **사전조건**: 영업 포털 로그인 완료.
- **테스트 단계**:
  1. 좌측 메뉴 **`데이터 탐색기`** 를 클릭한다.
  2. 상단 **테이블 선택** 드롭다운에서 테이블을 하나 선택한다.
  3. 데이터 그리드(표)가 표시되는지 확인한다.
- **기대 결과**: 선택한 테이블의 데이터가 그리드에 표시되고, 하단에 페이지네이션이 보인다.
- **Pass 기준**: 테이블 선택 시 데이터 그리드가 표시되면 합격.

---

### 2.39 데이터 탐색기 — 필터링/페이지 이동

- **목적**: 데이터 탐색기의 필터와 페이지 이동을 확인한다.
- **사전조건**: `데이터 탐색기` 화면, 테이블이 선택된 상태.
- **테스트 단계**:
  1. 상단 **필터 바**에서 조건을 지정해 데이터를 좁힌다.
  2. 하단 **페이지네이션**에서 다음 페이지로 이동한다.
- **기대 결과**: 필터 적용 시 그리드가 좁혀지고, 페이지 이동 시 다른 행 묶음이 표시된다. 요약 바(건수 등)가 갱신된다.
- **Pass 기준**: 필터/페이지 이동이 그리드에 반영되면 합격.

---

### 2.40 데이터 탐색기 — CSV/Excel 출력

- **목적**: 조회 결과를 CSV/Excel로 내보내는 동작을 확인한다.
- **사전조건**: `데이터 탐색기` 화면, 데이터가 조회된 상태.
- **테스트 단계**:
  1. 툴바의 내보내기 동작(CSV/Excel)을 실행한다.
- **기대 결과**: 현재 조회 결과가 파일로 다운로드된다.
- **Pass 기준**: 파일이 내려받아지면 합격. (라벨/버튼 위치는 툴바 영역에 있으며, 환경에 따라 다를 수 있음 — 출력 동작 여부로 판정)

---

### 2.41 데이터 탐색기 — 행 편집

- **목적**: 데이터 탐색기에서 행을 편집하는 동작을 확인한다.
- **사전조건**: `데이터 탐색기` 화면, 편집 가능한 테이블이 선택된 상태.
- **테스트 단계**:
  1. 그리드의 행에서 편집을 시작한다(행 클릭 → 우측 사이드 패널 또는 인라인 편집).
  2. 값을 수정하고 저장한다.
- **기대 결과**: 수정한 값이 그리드에 반영되고 저장 처리된다(서버 연동 시 DB 반영).
- **Pass 기준**: 편집·저장이 반영되면 합격. (읽기 전용 테이블이면 편집 불가가 정상)

---

### 2.42 알림 종(Notification Bell)

- **목적**: 상단 알림 종에서 알림 목록을 확인한다.
- **사전조건**: 영업 포털 로그인 완료.
- **테스트 단계**:
  1. 화면 상단(모바일 상단바 영역)의 **🔔 알림 종** 아이콘을 클릭한다.
  2. 알림 드롭다운이 열리는지 확인한다.
- **기대 결과**: 읽지 않은 알림이 있으면 종 옆에 숫자 배지가 표시되고, 클릭 시 알림 목록 드롭다운이 열린다.
- **Pass 기준**: 종 클릭 시 드롭다운이 열리면 합격. (알림 종은 상단바가 있을 때 표시됨)

---

### 2.43 비밀번호 변경 (본인 계정)

- **목적**: 로그인한 본인 계정의 비밀번호를 변경하는 동작을 확인한다.
- **사전조건**: 영업 포털 로그인 완료, 현재 비밀번호를 안다.
- **테스트 단계**:
  1. 사이드바 상단의 **`🔑 비밀번호 변경`** 링크를 클릭한다.
  2. "비밀번호 변경" 모달에서 **현재 비밀번호**와 **새 비밀번호(8자 이상)**를 입력한다.
  3. **`변경`** 버튼을 클릭한다.
- **기대 결과**: 변경 성공 메시지가 뜨고 모달이 닫힌다. 현재 비밀번호 불일치/짧은 새 비밀번호 등은 오류 메시지로 안내된다.
- **Pass 기준**: 올바른 입력 시 변경이 처리되고, 잘못된 입력 시 오류가 안내되면 합격.
  > 주의: 실제로 비밀번호를 바꾸면 다음 로그인에 영향이 있으니, UAT 시 변경값을 반드시 기록하거나 테스트 후 원복하세요.

---

### 2.44 로그아웃

- **목적**: 로그아웃이 정상 동작하는지 확인한다.
- **사전조건**: 영업 포털 로그인 완료. (다른 테스트를 모두 마친 뒤 마지막에 수행 권장)
- **테스트 단계**:
  1. 사이드바 상단의 **`↩ 로그아웃`** 링크를 클릭한다.
- **기대 결과**: 세션이 종료되고 로그인 페이지(`/login`)로 이동한다.
- **Pass 기준**: 로그인 화면으로 이동하면 합격.

---

### 2.45 사이드바 네비게이션 / 모바일 메뉴

- **목적**: 모든 메뉴 항목이 해당 페이지로 이동하고, 모바일에서 햄버거 메뉴가 동작하는지 확인한다.
- **사전조건**: 영업 포털 로그인 완료.
- **테스트 단계**:
  1. 사이드바의 각 메뉴(`영업 대시보드`/`영업 현황`/`거래처 관리`/`담당자 관리`/`학회 관리`/`영업사원 계정`/`📢 공지사항`/`사용량 입력`/`보고서 작성`/`데이터 탐색기`)를 차례로 클릭한다.
  2. 선택한 메뉴가 활성(active) 표시되고 해당 페이지가 뜨는지 확인한다.
  3. 브라우저 창을 좁히거나 모바일에서 좌상단 **햄버거(☰)** 버튼을 눌러 사이드바를 열고 닫는다.
- **기대 결과**: 각 메뉴 클릭 시 올바른 페이지로 전환되고 활성 표시가 따라온다. 모바일에서 햄버거로 사이드바가 열리고, 메뉴 선택 또는 오버레이 클릭 시 닫힌다.
- **Pass 기준**: 모든 메뉴가 올바른 페이지를 표시하고 모바일 메뉴 토글이 동작하면 합격.

---

## 스모크 체크리스트 (영업 포털 핵심 점검)

빠르게 핵심 기능만 점검하려면 아래 항목을 순서대로 확인하세요.

- [ ] `sales1@rtbio.com` 로 `http://localhost:3000/exec` 로그인 → 영업 대시보드 표시
- [ ] 영업 대시보드 상단 KPI 4장(월 매출/수금/미수금/담당 거래처)에 값이 채워짐
- [ ] `➕ 위젯 추가` → 갤러리에서 위젯 추가 반영
- [ ] `영업 현황` → 기간/유형 필터 `조회` 시 KPI·차트 갱신
- [ ] `영업 현황` 담당자 행 클릭 → 품목 드릴다운 필터 적용/해제
- [ ] `거래처 관리` 목록·검색 동작, 상세에서 품목별 판매수량 표 표시
- [ ] 거래처 상세 `비고 저장` → "비고 저장 완료" 토스트
- [ ] 거래처 상세 `발주서 출력` / `거래명세표 출력` 새 창 표시
- [ ] `담당자 관리` → `+ 거래처 배정` / `✕` 해제 동작
- [ ] `학회 관리` → `+ 학회 등록`, 학회 클릭 → 방명록 방문자 추가/삭제
- [ ] `영업사원 계정` → 사원 카드/상세, `발주` 버튼이 거래처 포털 새 창 오픈
- [ ] `📢 공지사항` → `＋ 공지 작성` 발송, 미리보기/편집/삭제 분리 동작
- [ ] `사용량 입력` → 당월사용 수정 시 소계/합계 자동 계산, `임시저장`/`제출` 토스트
- [ ] `보고서 작성` → 양식 선택 미리보기, `PDF 출력`/`인쇄` 동작
- [ ] `데이터 탐색기` → 테이블 선택·조회·필터·페이지 이동·내보내기
- [ ] `🔑 비밀번호 변경` 모달 동작 (검증 메시지 포함)
- [ ] `↩ 로그아웃` → 로그인 페이지 이동
