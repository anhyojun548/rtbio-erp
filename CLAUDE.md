# RTBIO ERP — 프로젝트 메모리

## 프로젝트
의료용품 업체 대상 멀티테넌트 SaaS ERP. 알티바이오 1곳으로 시작 → 3단계 성장 (1~5 → 5~30 → 30+곳).

## 기술 스택 (확정)
- **앱**: Next.js 14 (App Router) + TypeScript + Tailwind CSS + Zustand
- **DB**: PostgreSQL 16 + Prisma (스키마 분리형 멀티테넌시)
- **인증**: NextAuth.js + bcrypt
- **유틸**: Zod (검증), @react-pdf/renderer (거래명세서)
- **이메일**: Nodemailer + Daum SMTP (`smtp.daum.net:465` SSL) — 알티바이오 기존 daum.net 계정 사용, 월 1,000건 초과 시 Azure Communication Services Email / SendGrid 전환
- **호스팅**: Container 기반 (Vercel 등 서버리스 배제)
- **클라우드**: **Azure (Korea Central)** — 2026-04-15 확정
  - 앱: Azure Container Apps · DB: PostgreSQL Flexible Server B2ms · 파일: Blob Storage · CDN/WAF: Azure Front Door · 모니터링: Application Insights
  - 크레딧: Microsoft for Startups $6,000 신청 예정 (법인 등록 후)

## 도메인 규칙 (어기면 안 됨)

### 멀티테넌시
- `public` 스키마: User, Tenant, AuditLog (공용)
- `tenant_{id}` 스키마: Product, Order, Inventory (격리)
- 라우팅: `{tenant}.rtbio-erp.com` 서브도메인
- 쿼리 시 테넌트 컨텍스트 누락 금지 → 미들웨어로 강제

### 가격 계산 (주문 시점 스냅샷)
- 우선순위: `fixedPrices` > `discounts[category]` > `basePrice`
- 주문 테이블에 `unitPrice`, `basePriceAtOrder` 등 **스냅샷 컬럼 필수**
- 주문 확정 후 제품 가격이 바뀌어도 기존 주문은 영향 없음

### 재고 (이중 관리)
- `physicalStock`: 창고 실제 수량
- `availableStock`: 예약분 제외한 판매 가능 수량
- 주문 → 예약(availableStock 차감) → 출고(physicalStock 차감)
- 모든 변동은 `InventoryLog`에 기록 (감사)

### 반품 처리
- 별도 테이블 없음. `InventoryAdjustment`에 `reason='반품'` + `note` 로 처리
- 입고/출고와 동일한 상태 머신 사용

### 배송지 (복수 등록 + 스냅샷)
- 하나의 거래처는 여러 배송지를 등록할 수 있다 (`ClientAddress` 1:N)
- 거래처당 `isDefault=true` 는 **최대 1개** — 앱 로직으로 유일성 보장 (DB 제약 아님)
- 발주 시 배송지 선택 → `Order.shipTo{Label,Recipient,Phone,PostalCode,Address,AddressDetail,Memo}` **스냅샷 저장**
  - 가격 스냅샷과 동일 철학 — 이후 ClientAddress 가 수정/삭제돼도 과거 주문은 불변
- `Order.shipToAddressId` 는 참조용 (FK SetNull) — 스냅샷이 진짜 원본
- 임시 주소(등록 없이 이번 주문만) 도 허용: `shipToAddressId=null` + 스냅샷만 채움

## 아키텍처
- **포털 5개**: exec-portal(영업), admin-portal(경영지원), qc-portal(품질관리), client-portal(거래처), ceo-portal(대표)
- **공통 미들웨어**: 인증 → 테넌트 컨텍스트 → RBAC → 감사 로그
- **API 규약**: `/api/{domain}/...` + Zod 검증 + 표준 에러 포맷

## 코드 컨벤션
- TypeScript strict 모드, `any` 금지 (불가피 시 사유 주석)
- 감사 컬럼 누락 금지: `createdAt`, `updatedAt`, `createdBy`
- 외래키 CASCADE 정책 스키마 주석으로 명시
- TDD 원칙: 주요 비즈니스 로직은 테스트 먼저
- 커밋 메시지: 한국어 허용, 제목 50자 이내

## 디렉토리
```
docs/01-plan/       기획/스택 결정
docs/02-design/     API/DB/UI 설계
docs/03-analysis/   리뷰/분석
docs/04-report/     배포/운영 리포트
docs/superpowers/   플랜/스펙 (승인된 실행 문서)
prototype/          초기 HTML 목업
```

## 현재 단계
🟢 **Phase 3H (QC 포털 + 거래처 포털) 완료** (2026-04-19)
- Phase 1 ✅ 스키마 · 마이그레이션 · 시드 완료 (복수 배송지 포함)
- Phase 2 ✅ NextAuth + bcrypt + JWT · 포털별 RBAC 매트릭스 · AuditLog util
- Phase 3A ✅ 거래처 CRUD + 복수 배송지 UI · 검증자 · 감사로그 연결 (서버 액션)
- Phase 3B ✅ 제품 CRUD + 사이즈별 재고 초기값 + 유통기한 · `ClientType.PHARMACY` 추가
- Phase 3C ✅ 입고(RECEIVE) + 조정(반품/폐기/실사조정/입고보정) · `SELECT FOR UPDATE` 동시성 · 이중재고 불변식 · InventoryLog/Adjustment 분리 · 재고 현황/이력 페이지
- Phase 3D-1 ✅ 카테고리 할인율(ClientDiscount) + 제품 고정가(ClientFixedPrice) 업서트/삭제 · 거래처 상세에 두 패널 · pricing.ts 우선순위 스모크 검증
- Phase 3D-2a ✅ 주문 DRAFT CRUD + 라인 CRUD + 배송지 스냅샷 · pricing.ts 기반 라인별 단가 미리보기 · `/admin/orders` 목록·신규·상세 UI · 임시 orderNumber(`DRAFT-xxx`) · 25건 Vitest + smoke-order DB 파이프라인 검증
- Phase 3D-2b-1 ✅ SUBMIT 전환 · 공식 orderNumber 채번(`ORD-YYYYMMDD-NNN`, Postgres advisory lock 기반) · 라인 가격 재스냅샷 · billingMonth 세팅(R12) · `StatusActions` UI · 5건 Vitest(누적 120) + smoke-order-submit 검증(seq 증가/재SUBMIT 가드)
- Phase 3D-2b-2 ✅ REJECT · HOLD · RESUME · CANCEL(재고 미영향 경로) · `rejectedAt/Reason` · `heldAt/Reason` 컬럼 추가 · `applyStatusTransition` 헬퍼 · UI 사유 모달 + 상세 페이지 뱃지 · 16건 Vitest(누적 136) + smoke-order-transition 4 시나리오 통과
- Phase 3D-2b-3 ✅ CONFIRM(SUBMITTED→CONFIRMED) · 라인별 `SELECT FOR UPDATE` + availableStock 차감(RESERVE) · `InventoryLog.RESERVE/RELEASE` 기록 · 재고 부족 시 전체 롤백 · CANCEL 확장(CONFIRMED→CANCELLED 시 RELEASE) · UI 확정/취소 경고 모달 · 3건 Vitest(누적 139) + smoke-order-confirm 3 시나리오(avail 차감·복원·롤백) 통과
- Phase 3D-2c ✅ **Shipment 수명주기** — `startShipment`(CONFIRMED→SHIPPING, 첫 KanbanColumn 진입), `moveShipmentStage`(단계 이동 + ShipmentStageLog 기록, terminal 도달 시 자동 완료), `holdShipment`/`resumeShipment` · **SHIP 트랜잭션** — terminal 진입 시 라인별 `SELECT FOR UPDATE` + `physicalStock -= qty` + `InventoryLog.SHIP` + Order.status=COMPLETED · **칸반 UI** `/admin/shipments` (컬럼별 카드, 이동/보류/재개 인라인 모달) · **ShipmentCard** 실시간 갱신 · 15건 Vitest(누적 154) + smoke-shipment 4 시나리오(전체 수명주기·재-start 가드·hold/resume·중복 start 가드) 통과
- Phase 3D-3a ✅ **거래명세서(Invoice)** — COMPLETED 주문 → DRAFT Invoice 스냅샷 생성(`createInvoiceFromOrder`) · 주문당 활성 invoice 1건 제약 · VAT 10% 계산(`calcVatTotal`, R18) · `issueInvoice`(DRAFT→ISSUED, `INV-YYYYMMDD-NNN` Postgres advisory lock 채번, orderNumber 와 키공간 분리) · `markInvoiceSent`(ISSUED→SENT + sentAt) · `cancelInvoice`(사유 기록) · **PDF 출력** `/admin/invoices/[id]/pdf` (@react-pdf/renderer, Noto Sans KR CDN, 공급자/공급받는자/라인 테이블/VAT 합계) · **UI** `/admin/invoices` 목록+필터 · 상세(DRAFT 편집폼·상태 액션·PDF 미리보기) · 주문 상세(COMPLETED) "거래명세서 발급" 버튼 · 21건 Vitest(누적 175) + smoke-invoice 5 시나리오(생성·발행·중복 가드·취소·재생성 seq 증분) 통과
- Phase 3D-3b ✅ **수금(Payment) + 거래처원장(ClosingLedger)** — **Payment 서버액션** `recordPayment` / `updatePayment` / `cancelPayment`(소프트: status=PENDING + `[취소]` note 태그로 BankTxn FK 보존) / `sumPaymentsByClient`(PENDING 제외 합계) · **BankTransaction 서버액션** `createBankTxn` / `updateBankTxn`(매칭 상태면 거부) / `deleteBankTxn`(매칭 상태면 거부) / `matchBankTxn`(Payment.bankTxnId 세팅 + matched=true) / `unmatchBankTxn`(Payment.bankTxnId=null + matched=false) · **ClosingLedger 서버액션** `recomputeLedger`(carryOver=전월 balance, monthlySales=ISSUED/SENT Invoice.totalAmount 합, received=PARTIAL/PAID Payment.amount 합, balance=carry+sales-received · 마감 후 거부 · `(clientId, closingMonth)` unique + upsert 로 동시성 직렬화) / `recomputeLedgerMonth`(활성 거래처 일괄 · per-client 트랜잭션 실패 격리) / `closeMonth` + `reopenMonth`(사유 필수) · **UI** `/admin/payments`(수금 등록 + 목록 필터 + BankTxn 2테이블(미매칭·매칭)) · `/admin/ledger`(월 선택기 · 활성 거래처 × 월 표 · 이달 일괄 재계산 · 거래처별 재계산/마감/재개 · 합계 푸터) · 39건 Vitest(누적 214) + smoke-payment 6 시나리오(BankTxn 생성·매칭·해제·소프트 취소·원장 집계·마감→가드→재개) 통과
- Phase 3D-4a ✅ **유통기한 로트(ExpiryLot)** (R19) — **validators/expiry** `createExpiryLotSchema` / `updateExpiryLotSchema` + `classifyExpiry(expiryDate, now)` → `{ stage: EXPIRED|URGENT|SOON|SAFE, daysLeft }`(경계: <0 · ≤30 · ≤90 · >90) · **actions/expiry** `listExpiryLots` / `listExpiringSoon(90d)` / `getLotsForSize` / `createExpiryLot`(사이즈 내 `lotNumber` 중복 앱-레벨 가드 · `remainingQty=quantity` 초기화) / `updateExpiryLot`(`remainingQty ≤ quantity` 가드) / `deleteExpiryLot` · **UI** `/admin/expiry` 대시보드(4단계 stat 카드 + 단계/검색/빈로트 필터 + 인라인 편집/삭제) · 제품 상세 `LotsPanel`(추가 폼 · 정렬된 로트 테이블 · 단계 뱃지) · 16건 Vitest(누적 230) + smoke-expiry 5 시나리오(초기화·중복 가드·잔여 감소 및 초과 가드·4단계 분류·삭제) 통과
- Phase 3D-4b ✅ **출고내역 전용 조회 + CSV** (R17) — **actions/shipment** `listShipmentHistory({clientId, from, to, q, limit})`(completedAt != null 기본 · `order.client.id/name/code` + `orderNumber` 대소문자 무시 부분일치 · `from/to` 합성 필터 · order.items 집계 포함) · **UI** `/admin/shipments/history`(건수/총수량/총금액 3장 stat 카드 + 필터 바(거래처 select · from/to date · 검색 input) + 완료일 desc 테이블 + 주문 상세 딥링크 + 사이드바 추가) · Sidebar 활성 규칙 개선(longest-prefix) — `/admin/shipments` 와 `/admin/shipments/history` 동시 활성 해결 · **CSV route** `/admin/shipments/history/csv`(UTF-8 BOM + CRLF · 13컬럼(완료일시/주문번호/주문일/거래처코드/거래처명/제품코드/제품명/사이즈/수량/라인합계/수령인/배송지/정산월) · RFC4180 쿼팅/더블쿼팅) · smoke-shipment-history 5 시나리오(기본조회 desc 정렬 · clientId 필터 · 기간 분할 · q prefix(orderNumber)/clientCode 부분일치 · CSV BOM+쿼팅) 통과
- Phase 3D-4c ✅ **월간 보고서** (R16) — **actions/report** `computeMonthlyReport(closingMonth)` / `getMonthlyReport(closingMonth)` / `getMonthlyReportWithPrev(closingMonth)` · `monthToRange` 기반 [월초, 다음달초) 범위로 `Invoice.issueDate` / `Payment.paidAt` / `Shipment.completedAt` 동시 집계 · **매출 total** = ISSUED+SENT(DRAFT·CANCELLED 제외, byStatus 는 전체 분포) · **수금 total** = PARTIAL+PAID(PENDING·OVERDUE 제외, ClosingLedger 정의와 일치) · **Shipment total** = 완료건수 + 라인 qty/amount 합 · **Top 10 거래처** = ISSUED/SENT 매출 desc · **원장 요약** = `listLedgers(closingMonth)` 의 `carry/sales/received/balance` 합 + 마감건수 · **UI** `/admin/reports/monthly?month=YYYY-MM`(월 input + 전/다음달 버튼 · 매출/수금/출고/미수금 4장 stat 카드에 전월 대비 ▲▼ 델타 + 퍼센트 · Invoice/Payment 상태별 분포 테이블 · Top 10 거래처 테이블 + 비중 · 원장 요약 푸터(carry+sales-received=balance) + 원장/거래명세서 딥링크) · Sidebar "월간 보고서" 추가 · smoke-monthly-report 2 시나리오(빈 월 0집계 · 혼합 fixture 정확 집계 — invoice 2건 7700 / payment 2건 5200 / ship 2건 qty=7·amt=7000 / Top [B=5500, A=2200] / ledger carry=1500+sales=7700-received=5200=balance=4000) 통과
- Phase 3E-3 ✅ **재고 임계치 알럼** (R14) — **validators/stock-alert** `classifyStock({physicalStock, availableStock, reorderPoint})` → `{level: OUT|LOW|OK, deficit}` 순수 함수(경계: physicalStock≤0 → OUT, reorderPoint null/≤0 && stock>0 → OK, 0<stock≤reorderPoint → LOW) · `compareStockUrgency(a,b)`(OUT < LOW < OK, 같은 level 은 deficit desc) · `STOCK_LEVEL_LABEL` 한글 맵 · **actions/stock-alert** `listLowStockAlerts({limit?})`(활성 제품 × reorderPoint>0 사이즈 전체 로드 후 앱-레벨 분류 · OUT 먼저 deficit desc 정렬 · 선택적 limit) / `countStockAlerts()`(OUT/LOW/OK/totalActiveSizes 집계, 대시보드 배지용) · **UI** `/admin/alerts/stock` 전용 페이지(4장 stat 카드 — 품절/부족/정상/활성합계 · 레벨 토글 필터(전체·OUT·LOW) · 제품코드/카테고리/브랜드/사이즈 검색 · 제품 상세 딥링크) · 대시보드 `/admin` 에 "재고 임계치 알럼" 섹션 추가(Top 5 + 뱃지 · "전체 보기" 링크 · 알럼 0건 시 긍정 메시지) · Sidebar "재고 알럼" 추가 · 11건 Vitest(누적 272) + smoke-stock-alert 6 시나리오(OUT d10 · LOW d5 · OK · reorderPoint null && stock=0=OUT d0 · 정렬 A,B,F · countStockAlerts OUT=2/LOW=2/OK=1) 통과
- Phase 3E-2 ✅ **테넌트 설정** (R13) — **validators/tenant-setting** `TENANT_SETTING_KEYS`(business_hour_start/end · shipping_cutoff · reorder_multiplier · vat_rate 5개 리터럴) · 키별 value 스키마 맵(HH:MM 24h regex · 양수 소수 · 0~1 ratio) · `updateSettingSchema`(superRefine 로 키별 값 스키마 분기) · `bulkUpdateSettingsSchema`(1~5개, 키 중복 refine, 항목별 값 검증) · `validateBusinessHours(start,end)` 순수 함수 · **actions/tenant-setting** `listSettings`(알려진 5키를 정렬해 value/updatedAt/updatedBy 반환 · 미설정 키는 value=null) / `getSettingValue(key)` / `getSettings()` 맵 / `updateSetting` upsert / `bulkUpdateSettings`(tx 안 upsert + 업무시간 start<end 비즈니스 규칙 DB 값과 결합 검증) · 모든 쓰기에 `logAudit({action: "TENANT_SETTING_UPDATE|BULK_UPDATE"})` · **invoice.ts** VAT 계산이 `vat_rate` 세팅을 읽어 `calcVatTotal(supply, rate)` 2번째 인자로 전달(기본값 0.1 유지, 기존 21건 invoice 테스트 그대로 통과) · **UI** `/admin/settings` 설정 페이지(키별 input type — time/number · "변경됨" 뱃지 · 변경 항목만 저장 · 초기화 버튼 · 서버 에러 메시지 표시) · Sidebar "테넌트 설정" 추가 · 18건 Vitest(누적 261) + smoke-tenant-setting 5 시나리오(시드 확인 · 단일 upsert · 3키 일괄 · VAT 재계산 0.08=800/기본 0.10=1000 · 업무시간 규칙) 통과
- Phase 3E-1 ✅ **칸반 단계 커스터마이징** (R05) — **validators/kanban** `createKanbanColumnSchema`(key 대문자+숫자+언더스코어 regex · label trim 1~50 · sortOrder 0~999 · isTerminal default false · color "#RRGGBB" 선택값, 빈 문자열→undefined transform) / `updateKanbanColumnSchema`(전 필드 optional) / `reorderKanbanColumnsSchema`(items 1~32, id 고유 refine) · **actions/kanban** `listKanbanColumnsWithUsage`(각 컬럼에 `_count.shipments` include) / `createKanbanColumn`(앱-레벨 중복 key 가드 + DB P2002 폴백) / `updateKanbanColumn`(각 필드 optional, 감사) / `deleteKanbanColumn`(`_count.shipments > 0` 시 한글 에러, P2003 폴백) / `reorderKanbanColumns`(tx 안에서 존재 검증 후 bulk update) · 모든 변경에 `logAudit({action: "KANBAN_COLUMN_*", resource, metadata})` · **UI** `/admin/shipments/columns` 관리 페이지(단계 추가 폼 · 인라인 편집 · ▲▼ 재정렬 버튼 · 삭제 가드(연결된 shipment 수 표시) · terminal 단계 뱃지) · 칸반 보드 헤더에 "⚙ 단계 관리" 링크 추가 · 13건 Vitest(누적 243) + smoke-kanban-columns 5 시나리오(기본값 isTerminal=false · unique 가드 · update 반영 · reorder asc 정렬(C/B/A) · delete + _count 기반 가드) 통과
- Phase 3F-1 ✅ **영업 포털 골격** (R11) — **actions/exec** `getMyClientIds(salesRepId)` 내부 헬퍼(Client.salesRepId 직접담당 ∪ SalesAssignment{active:true} 복수배정, active 거래처만, Set 중복 제거) / `listMyClients({forUserId?})`(내 거래처 + 이달 ISSUED+SENT 매출합 + 최근 주문 1건 + _count 주문/배송지) / `getMyOrderSummary({forUserId?, recentLimit=10})`(상태별 groupBy + 최근 N건 — items.lineTotal 합산으로 totalAmount 계산) / `getMyTopClientsByMonth({forUserId?, month?, limit=10})`(Top 10 거래처 이번달 ISSUED+SENT 매출 desc) / `listMyOrders({forUserId?, q, status, from, to, limit=200})`(필터 기반 주문 목록 — items.lineTotal 집계) · TENANT_OWNER/ADMIN 은 `forUserId` 로 대리 조회 가능 · **UI** `/exec/layout.tsx`(TopBar + ExecSidebar 공통) · `/exec/page.tsx` 대시보드(4장 stat 카드 — 내 거래처/진행 주문(DRAFT+SUBMITTED+CONFIRMED)/완료 주문/이번 달 매출 · Top 5 거래처 · 최근 5건 주문 + StatusBadge) · `/exec/clients` 내 거래처 테이블(검색/유형 필터/정렬 — 이름·누적 주문·이달 매출·최근 주문) · `/exec/orders` 내 주문 테이블(검색/상태/기간 필터 · useTransition + URLSearchParams) · **컴포넌트** `ExecSidebar`(longest-prefix active detection) · `MyClientsTable` · `MyOrdersTable` · RBAC: TENANT_OWNER/ADMIN/EXEC · smoke-exec 6 시나리오(me={C1 직접,C2 active 배정}·C3 비활성 배정 제외 · listMyOrders 필터 O1/O2 만 · 이달 매출 합 C1+C2=33000 · other 대리 조회 C2+C3=55000) 통과 · 전체 272건 Vitest 통과
- Phase 3F-2 ✅ **담당자별 매출** (R15) — **actions/sales-performance** `getClientIdsForRep` 내부 헬퍼(exec.ts 와 동일 union 규칙) / `computeSalesPerformance(closingMonth)`(활성 EXEC/ADMIN/TENANT_OWNER 전원 × 담당 거래처 매출(Invoice ISSUED+SENT) / 입금(Payment PARTIAL+PAID) / 미수금(ClosingLedger.balance 합) / 주문수(Order.createdAt 월 필터) 집계 · 매출 desc 정렬) / `listSalesPerformanceByMonth`(세션 게이트) · `computeClientBreakdown({salesRepId, month})`(거래처별 매출/입금/미수금 행, sales desc) · `computeProductBreakdown({salesRepId, month})`(COMPLETED 주문의 items 집계, `productId::sizeId` 키로 qty/amount 합산, amount desc) · 각각 `getSalesRepClientBreakdown` / `getSalesRepProductBreakdown` 게이트 · **UI** `/admin/reports/sales?month=YYYY-MM`(담당자별 성과 표 — 번호·이름·롤·거래처수·주문수·매출·입금·미수금 · 합계 푸터 · 담당자 링크 → 상세) · `/admin/reports/sales/[salesRepId]`(4장 stat + 거래처별 breakdown + 제품/사이즈별 breakdown with 비중 %) · 재사용 `MonthPicker` 컴포넌트 추출 (전/다음달 버튼 + input[type=month]) · Sidebar "담당자별 매출" 추가 · smoke-sales-performance 4 시나리오(repA C1+C2 sales=30000/pay=20000/out=10000 · repB C2+C3 sales=50000/pay=35000/out=15000 · desc 정렬(repB>repA) · breakdown C2>C1 · product 집계 qty=8 amount=8000) 통과 · 전체 272건 Vitest 통과
- Phase 3G-1 ✅ **데이터 사용량 입력** (R22) — **validators/data-usage** `DATA_USAGE_CATEGORY_PRESETS` 10개 · `DATA_USAGE_UNIT_PRESETS` 8개 · `monthField`(YYYY-MM regex) · `createDataUsageSchema`(amount z.coerce.number nonnegative, note transform 빈값→undefined) / `updateDataUsageSchema`(부분 패치, note nullable) / `upsertDataUsageSchema` · `prevMonthString(YYYY-MM)` / `computeMoMDelta(current, previous)` 순수 함수 · **actions/data-usage** `listDataUsage({month, category, limit})` / `listAvailableMonths(limit=24)` / `getMonthSummary(month)`(카테고리별 합·count) / `getMonthWithPrev(month)`(당월/전월 union 비교 — 신규·누락 카테고리 모두 포함) / `createDataUsage`(P2002 중복 시 한글 폴백) / `upsertDataUsage`(month+category 복합 unique 기준 덮어쓰기, 감사에 create/update 구분) / `updateDataUsage`(P2025 폴백) / `deleteDataUsage` · 모든 쓰기 `logAudit({action: "DATA_USAGE_*"})` + revalidatePath · **UI** `/admin/data-usage?month=YYYY-MM`(MonthPicker 재사용 · 최근 기록 퀵 버튼 · 3장 stat 카드 · 당월/전월 비교 테이블(증감 % · 신규/누락 뱃지) · 당월 상세 테이블 인라인 편집/삭제 · 추가 폼 with datalist 프리셋) · Sidebar "데이터 사용량" 추가 · 18건 Vitest(누적 308) + smoke-data-usage 6 시나리오(전월+당월 6건 생성 · P2002 중복 가드 · upsert 덮어쓰기 120→130 · MonthSummary 합=195 · MonthWithPrev 4카테고리(신규·누락 포함) · delete 후 2건) 통과
- Phase 3G-2 ✅ **판매 계약서** (R20) — **schema** `SalesContract` ↔ `Client` 양방향 relation + `onDelete: Cascade` · `@@index([endDate])` 추가(migration `20260419032834_sales_contract_relation`) · **validators/sales-contract** `CONTRACT_STATUS` 4단계 리터럴(ACTIVE·ENDING_SOON·EXPIRED·FUTURE) + 한글 라벨 맵 · `createContractSchema`(clientId cuid · title 1~200 · startDate/endDate z.coerce.date · pdfUrl http/https refine + 빈값→undefined transform · signed default false · note 0~1000 · superRefine endDate≥startDate) / `updateContractSchema`(전 필드 optional · endDate/pdfUrl/note nullable · startDate+endDate 동시 변경 시 순서 검증) · `classifyContract(startDate, endDate, now)` 순수 함수(미래 시작 → FUTURE · endDate null → ACTIVE 무기한 · daysLeft<0 → EXPIRED · ≤30 → ENDING_SOON · 그 외 ACTIVE) · **actions/sales-contract** `listContracts({clientId, signed, q, limit})`(q 는 title + client.name + client.code insensitive) / `getContract(id)` / `listExpiringSoon(days=30)`(endDate 가 [today, today+days] 범위) / `countContractStatuses()`(4단계 집계) / `createContract`(거래처 존재 확인, P2003 폴백) / `updateContract`(단일 변경 시 DB 현재값과 교차 검증으로 순서 체크 · P2025 폴백) / `deleteContract`(P2025 폴백) · 모든 쓰기 `logAudit({action: "CONTRACT_*"})` + `revalidatePath("/admin/contracts" + /admin/clients/{id})` · **UI** `/admin/contracts`(4장 stat 카드(ACTIVE/ENDING_SOON/EXPIRED/FUTURE) + ContractFilterBar(q·상태·서명) + 테이블(제목·거래처·시작일·종료일·상태 뱃지(남은/경과일)·서명·PDF 링크·상세 링크)) · `/admin/contracts/new?clientId=?`(ContractForm mode=create with 활성 거래처 select) · `/admin/contracts/[id]`(상단 상태 뱃지 + ContractForm mode=edit(거래처 변경 잠금) + 감사 푸터) · 거래처 상세(`/admin/clients/[id]`)에 "판매 계약서" 섹션 추가(목록 + "+ 신규 계약" with clientId 프리필) · 대시보드 `/admin` 에 "만료 임박 30일" 위젯 추가(상위 5건 · 전체 보기 링크) · Sidebar "📝 판매 계약서" 추가 · 25건 Vitest(누적 333) + smoke-sales-contract 5 시나리오(거래처 1 × 4건(ACTIVE/ENDING_SOON/EXPIRED/FUTURE) · classifyContract 경계 4단계 · signed 토글 · endDate→null 무기한 전환(ENDING_SOON→ACTIVE) · Client 삭제 → SalesContract CASCADE 4→0) 통과
- Phase 3G-4 ✅ **CEO 대시보드 위젯 커스터마이징** (R24) — **스키마** 기존 `DashboardWidget`(userId/preset/position/width/height/overrideDateRange/config) 재사용 · **validators/widget-dashboard** `DASHBOARD_WIDGET_PRESETS` 10종(KPI 6 — 이번달매출·미수금·진행주문·활성거래처·재고알림·만료임박계약 / LIST 4 — Top5거래처·재고부족Top5·만료임박계약Top5·최근주문5) with {key, label, description, kind, defaultWidth/Height, icon} · `PRESET_BY_KEY` 맵 + `isValidPresetKey`/`getPreset` 헬퍼 · `DEFAULT_LAYOUT_KEYS` 4 KPI · `addWidgetSchema`(preset refine + position/width/height optional) / `updateWidgetSchema`(id cuid + width/height 1~12 + overrideDateRange nullable transform '' → null) / `reorderWidgetsSchema`(items 1~20, id 고유 refine) / `resetLayoutSchema`(presetKeys optional, 전부 유효 key refine) · `DATE_RANGE_PRESETS` 5종(today/last7/last30/month/last_month) + `isValidDateRange` · **actions/widget-dashboard** `requireCeoUser()`(TENANT_OWNER/ADMIN/SUPER_ADMIN) / `listMyWidgets()`(position asc + meta 조인) / `addWidget`(position 생략 시 max+1 append, 기본 width/height 는 프리셋 default) / `updateWidget`(소유권 검증, 부분 패치) / `removeWidget`(소유권 검증) / `reorderWidgets`(tx 안에서 소유권 일괄 검증 후 per-row update) / `resetLayout`(tx 안 deleteMany + DEFAULT_LAYOUT_KEYS insert) / 모든 쓰기 `logAudit({action: "DASHBOARD_WIDGET_*|DASHBOARD_LAYOUT_RESET"})` + `revalidatePath("/ceo", "/ceo/customize")` · **프리셋 값 계산** `computePresetValue(preset, now)` switch 로 10종 분기 — invoice `monthToRange` + ISSUED/SENT / ClosingLedger balance sum / Order status.in(DRAFT+SUBMITTED+CONFIRMED+SHIPPING) / Client active=true count / ProductSize + `classifyStock` OUT+LOW / SalesContract endDate≤30d / Top5 (groupBy in-memory, amount desc) / Low stock (`classifyStock` OUT 먼저 deficit desc) / 만료 임박 (endDate asc take 5) / 최근 주문 (createdAt desc take 5) · `computeAllPresetValues(keys[])` 병렬 실행 · `getMyDashboard()` 번들 · `getAllPresets()` 카탈로그 · **UI** `/ceo/page.tsx`(TopBar + 12-col grid with `gridColumn: span N`, 빈 레이아웃 시 "편집으로 이동" CTA, "⚙️ 위젯 편집" 헤더 버튼) · `/ceo/customize/page.tsx`(뒤로 링크 + `WidgetCustomizer`) · **컴포넌트** `WidgetCard`(KPI — 아이콘/라벨/3xl 값/설명 · LIST — 헤더+테이블 · href 있으면 Link 래핑) · `WidgetCustomizer`(내 위젯 ul with ▲▼ 이동 + width select(3/4/6/8/12) + 기간 override select(5 프리셋) + 제거 버튼 · 추가 가능 프리셋 grid with kind 뱃지 · "기본 레이아웃 초기화" 버튼 · useTransition + router.refresh + 에러 배너) · 17건 Vitest(누적 361) + smoke-widget-dashboard 7 시나리오(유저 생성/초기 0건 · append 3건 position 0/1/2 · reorder → [sales, open_orders, total_ar] · update width=6 + override='month' · remove 가운데 → 2건 · resetLayout 기본 4 KPI · computePresetValue 10종 kind/label 일치) 통과
- Phase 3G-3 ✅ **기간별 영업 이력서** (R21) — **validators/sales-history** `SALES_EVENT_TYPES` 4단계(ORDER_CREATED·INVOICE_ISSUED·PAYMENT_RECEIVED·CONFERENCE_VISITOR) + 한글 라벨/아이콘 맵 · `salesHistoryQuerySchema`(salesRepId cuid · from/to z.coerce.date · superRefine from≤to) · `dateRangeToWindow(from, to)`(`[from 00:00, to+1일 00:00)` 반개구간) · `parseYmd(s)`(YYYY-MM-DD 엄격 정규식 + 월일 유효성) · `defaultRange(now)`(이번달 1일 ~ 오늘 YYYY-MM-DD) · **actions/sales-history** `getClientIdsForRep` 내부 헬퍼(exec.ts 와 동일 union — Client.salesRepId 직접 ∪ SalesAssignment{active:true}) / `computeSalesHistory({salesRepId, from, to})`(세션 우회, 스모크/테스트용 · 4종 이벤트 동시 집계 · Order.createdAt / Invoice.issueDate(ISSUED+SENT) / Payment.paidAt(PARTIAL+PAID) / ConferenceVisitor.createdAt(assignedRepId=rep) · totals + byClient(invoiceAmount desc) + events(occurredAt desc 평탄화)) / `getSalesHistory(opts)` 세션 게이트(EXEC 은 본인만, `salesRepId != me.id` 시 거부) / `listAssignableSalesReps()`(TENANT_OWNER/ADMIN 전체 active EXEC/ADMIN/TENANT_OWNER, EXEC 본인만) · **UI** `/admin/reports/sales-history?repId=...&from=...&to=...`(쿼리 파라미터 → 기본값 fallback · 담당자 select + 시작/종료일 input + "조회" · 4장 stat 카드(주문/명세서/수금/방문자) · 거래처별 활동 테이블(invoice desc) · 이벤트 타임라인 + 타입 필터 pill(전체+4종) + 이벤트 클릭-스루 링크(주문/명세서/수금)) · **컴포넌트** `SalesHistoryBoard`(Client Component, useTransition + URLSearchParams · fmtDateTime ISO slice · eventHref switch) · 서버→클라 전달 시 Date → ISO 문자열 직렬화 · Sidebar "🗓️ 영업 이력서" 추가 · 11건 Vitest(누적 344) + smoke-sales-history 7 시나리오(rep + C1 직접/C2 배정/C3 미담당 · 주문 4건(범위 내 3 + 밖 1) · Invoice ISSUED+SENT 2 + DRAFT 제외 · Payment PARTIAL+PAID 2 + PENDING 제외 · Visitor 범위 내 2 + 밖 1 · totals 3/35000·2/27500·2/23500·2 · byClient C2(16500) > C1(11000,2) · events 9건 desc · C3 미포함) 통과
- Phase 3H-1 ✅ **QC 포털** (R02·R03·R09·R14·R17·R19) — **RBAC** `requireRole("QC", "ADMIN", "TENANT_OWNER")` · **레이아웃** `/qc/layout.tsx` + `QcSidebar`(longest-prefix active — 대시보드·출고 칸반·출고 내역·샘플 출고·재고 현황·재고 알럼·유통기한·설정) · **대시보드** `/qc` — 재고 임계치(OUT/LOW) Top 5 + 유통기한 위험(EXPIRED/URGENT) Top 5 + 당일 완료 출고 건수 · **출고 칸반** `/qc/shipments`(admin 과 동일 기능 재사용) · **출고 내역** `/qc/shipments/history`(admin 재사용 · 읽기전용) · **샘플 출고** `/qc/samples`(SampleShipForm 클라이언트 폼 — 제품·사이즈·거래처·수량·사유 · `createInventoryAdjustment` 직접 호출 · `reason="샘플출고"` + 음수 qty) · **재고 현황** `/qc/inventory`(admin 재사용) · **재고 알럼** `/qc/alerts`(admin/alerts/stock 재사용) · **유통기한** `/qc/expiry`(admin/expiry 재사용) · **설정** `/qc/settings`(테넌트 설정 — QC 도 편집 가능) · **validators/inventory** `adjustmentSchema` 에 "샘플출고" 추가 + 음수-only refine(폐기와 동일) · 16건 Vitest(누적 362 — inventory 15→16)
- Phase 3H-2 ✅ **거래처 포털** (R01·R08) — **스키마** `User.clientId String?` 추가(+ `@@index`) · 크로스스키마 FK 회피(앱-레벨 참조) · 마이그레이션 `20260419053654_user_client_link` · **시드** AGENCY 거래처별 CLIENT 유저 자동 생성(`${code.toLowerCase()}@client.local` / "altibio123!@#" / role=CLIENT / clientId 연결) · **NextAuth 확장** `Session.user.clientId` · `authorize`/`jwt`/`session` 콜백에 clientId 전파 · **session.ts** `requireClient()`(role=CLIENT && clientId 검증 → /403) · **actions/client-portal** 읽기-전용 10개 — `getMyClient` / `getMyDashboard`(진행주문/완료주문/이달매출(ISSUED+SENT)/미수금(최근원장 balance)/최근주문 5/최근명세서 5/만료임박 계약 30d) / `listMyOrders`(q·status·from·to · OPEN 가상 상태) / `getMyOrder`(shipment.currentStage · items · invoices DRAFT 제외) / `listMyInvoices`(DRAFT 제외) / `getMyInvoice`(소유권 + DRAFT 제외) / `listMyPayments`(PARTIAL/PAID/OVERDUE) / `listMyLedgers` / `listMyContracts` / `getMyContract` · 모든 쿼리 `clientId` 행-레벨 필터 강제 · **UI** `/client/layout.tsx`(TopBar + ClientSidebar, 거래처명 표시) · `/client/page.tsx`(4장 stat 카드 + 최근 주문/명세서 2열 + 만료 임박 계약 알럼) · `/client/orders`(3장 stat + q·status·from·to 필터 + StatusBadge) · `/client/orders/[id]`(배송지·칸반 타임라인·품목·연결 명세서, 보류/반려 사유 뱃지) · `/client/invoices`(4장 stat(건수·공급가액·VAT·합계) + 필터 + PDF 링크) · `/client/invoices/[id]`(공급자/공급받는자 카드 + 품목 테이블 + 합계 푸터 + PDF 버튼) · `/client/invoices/[id]/pdf/route.tsx`(`@react-pdf/renderer` 재사용 · 소유권 + ISSUED/SENT/CANCELLED 게이트) · `/client/payments`(4장 stat(당월 미수금·매출·수금·90일 수금건수) + 월별 원장 24개월 테이블 + 수금 내역 100건) · `/client/contracts`(`classifyContract` 뱃지 D-N · q 검색) · `/client/contracts/[id]`(상태 뱃지 + 날짜·서명·PDF·메모 + 감사 푸터) · `/client/profile`(읽기전용 거래처 정보 + 배송지 리스트 + 4장 count 카드) · Sidebar 6개 메뉴(대시보드·발주·명세서·수금·계약서·내 정보) · 시드 3건 CLIENT 유저 생성 · 전체 362건 Vitest + tsc 클린 통과
- Phase 3F-3 ✅ **학회 방명록** (R21) — **validators/conference** `VISITOR_CONTACT_STATUS` 4단계 리터럴(NEW·CONTACTING·DEAL·LOST) + 한글 라벨 맵 · `createConferenceSchema`(name 1~100 · location 0~200 · startDate/endDate z.coerce.date · note 0~1000 · superRefine endDate >= startDate) / `updateConferenceSchema`(전 필드 optional · nullable 규칙 · 동일 endDate 가드) / `createVisitorSchema` / `updateVisitorSchema`(assignedRepId optional cuid / nullable · contactStatus enum 관대 처리 — 알 수 없는 값은 undefined 로 transform) · **actions/conference** `listConferences({q, upcoming, from, to, limit})`(q 는 name/location contains insensitive · upcoming 은 startDate>=오늘 00:00 · _count.visitors 포함) / `getConference(id)`(visitors desc include) / `createConference`(name/location/startDate/endDate/note + createdBy 감사) / `updateConference`(P2025 한글 폴백) / `deleteConference`(CASCADE — 스키마의 onDelete:Cascade 로 ConferenceVisitor 자동 제거) / `createVisitor`(학회 존재 확인 + assignedRepId 활성 체크) / `updateVisitor`(동일 활성 체크 + optional 패치) / `deleteVisitor` / `listAssignableReps`(active EXEC/ADMIN/TENANT_OWNER, name asc) · 모든 쓰기에 `logAudit({action: "CONFERENCE_*|CONFERENCE_VISITOR_*"})` + `revalidatePath` · **UI** `/exec/conferences`(학회 카드 그리드 · q/upcoming 필터 · 📅 일정 · 📍 장소 · 방문자 수 · 예정/종료 뱃지) · `/exec/conferences/new`(ConferenceForm mode=create) · `/exec/conferences/[id]`(상단 ConferenceForm mode=edit + 하단 VisitorPanel) · **컴포넌트** `ConferenceForm`(생성/수정/삭제, 날짜 필드 new Date() 변환) · `VisitorPanel`(방문자 테이블 + 인라인 추가/편집 폼 · 담당자 select · 상태 select with color badge · useTransition + router.refresh) · ExecSidebar "🎓 학회 방명록" 추가 · 18건 Vitest(누적 290) + smoke-conference 6 시나리오(2건 생성 · upcoming 필터(A만) · 검색 '소아'(B만) · 비활성 rep 배정 거부 · 담당자 제거+상태 전환 · Conference 삭제 → Visitor CASCADE) 통과

**프로토타입·과업내용서는 계약 기준 유지** (계약 체결됨, 실개발 진행).

**완료된 산출물**
- 프로토타입 5개 포털 + Step A/B/C 보완 완료
- 팀별 배포본 5개 (`prototype/teams/*/`) — HTML + 사용설명서 + 로컬 리소스
- 과업내용서 2종: `docs/3. 과업내용서_RTBIO_{간소화,상세}_260417.docx`
  - 간소화: 업무영역 단위 (분쟁 예방형)
  - 상세: R01~R24 명세 (스코프 확정형)

**다음 일정**
1. 4/21 1차 데모 (QC·경영지원 집중)
2. 4/27 2차 데모 (영업·거래처·CEO)
3. ~4/30 **계약 체결** (과업내용서 1종 선택 발송)
4. 5/4~ **Phase 1 실개발 착수** (Prisma 스키마)

**단일 진실 원천**: `docs/superpowers/plans/2026-04-18-master-plan.md` (타임라인·기능범위·Phase 로드맵)

**원칙**: 계약 체결 전까지 백엔드/DB/배포 코드 금지. `prototype/*.html` + Mock JSON 으로만 검증.

## 하네스 운영 원칙
1. 서브에이전트 결과는 200단어 이내 요약으로 받기
2. 같은 파일 쓰는 작업은 순차, 독립 작업은 병렬
3. 메인은 조정/통합만, 구현은 서브에 위임
4. 도메인 결정(가격/재고/반품 규칙)은 본 문서 기준 — 재확인 금지
5. Phase 경계마다 `/learner` 실행 → `.omc/skills/` 축적
6. 커스텀 에이전트는 `schema-designer`, `pricing-specialist`, `inventory-specialist` 3개로 고정 (추가 금지)
